<!DOCTYPE html>
<html lang="en">
<head>
  <title>CloudLogr</title>
  <style>
    body {
      background-image: url("./images/tank_farm.jpg");
      background-size: cover;
      background-repeat: no-repeat;
      text-align: center;
    }
    table, th, td {
        border: 2px solid black;
        padding: 4px;
        text-align: center;
        background: #efefef;
        margin: 0 auto;
    }
    button.button_poll {
      position: relative;
      top: 50px;
      left: -253px;
    }
  </style>
  <script src="/javascripts/tank.js" type="text/javascript" charset="utf-8" async defer></script>
</head>
<body>
  <h1>Welcome to CloudLogr</h1>
  <!-- <button class="button_poll" type="">Poll: <%= schedule.dataPollRate %>ms</button> -->
  <table class="tank_table">
    <caption><h3><%= schedule.dataTagname %></h3></caption>
    <thead>
      <tr>
        <th colspan="" rowspan="" headers="" scope="">Value</th>
        <th colspan="" rowspan="" headers="" scope="">Timestamp</th>
        <th colspan="" rowspan="" headers="" scope="">Read Status</th>
        <th colspan="" rowspan="" headers="" scope="">Error Message</th>
      </tr>
    </thead>
    <tbody>
      <% schedule.data = schedule.data.reverse(); %><!--want newest first to rendering table-->
      <% schedule.data.slice(0,10).forEach(function(dataEntry){ %> <!-- build the table from 10 most recent entries data -->
       <tr>
        <td colspan="" rowspan="" headers=""><%= dataEntry.value %></td>
        <td colspan="" rowspan="" headers=""><%= dataEntry.timestamp %></td>
        <td colspan="" rowspan="" headers=""><%= dataEntry.status ? 'good' : dataEntry.statusCode %></td>
        <td colspan="" rowspan="" headers=""><%= dataEntry.eWONMessage %></td>
        </tr>
      <% }); %>
    </tbody>
  </table>
  <h3>Poll Rate: <%= schedule.dataPollRate %>ms.</h3>
  <h3>Polling State: <%= schedule.dataPollingState ? 'Active' : 'Inactive' %></h3>
  <div class="table_buttons">
    <form action="/api/v2/startPolling" method="post" accept-charset="utf-8">
      <input type="hidden">
      <input type="submit" value="Start Polling">
    </form>
    <form action="/api/v2/stopPolling" method="post" accept-charset="utf-8">
      <input type="hidden">
      <input type="submit" value="Stop Polling">
    </form>
    <form action="/api/v2/changePollRate" method="post" accept-charset="utf-8">
      Poll Rate:
      <input type="text" name="newDataPollRate" value="5,000 - 60,000 (ms)" onFocus="clearText(this)">
      <input type="submit" value="Submit">
    </form>
  </div>
  <!-- for socket io -->

  <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/1.4.5/socket.io.js"></script>

  <script src="/socket.io/socket.io.js"></script>

  <script>
    var socket = io();
    socket.on('connect', function() {
    console.log('Connected on front end.');
});
  </script>
  <script src="https://code.jquery.com/jquery-2.2.4.min.js" integrity="sha256-BbhdlvQf/xTY9gja0Dq3HiwQF8LaCRTXxZKRutelT44=" crossorigin="anonymous"></script>
</body>
</html>
